name: CD

on:
  push:
    branches: ["main", "develop", "cicd"]
    paths:
      - "src/**"
      - "build.gradle*"
      - "settings.gradle*"
      - "Dockerfile"
      - "docker-compose.yml"
      - ".github/workflows/cd.yml"
  workflow_dispatch: {}

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    # 이중 안전장치: 브랜치 가드
    if: contains('refs/heads/main,refs/heads/develop,refs/heads/cicd', github.ref)
    name: Build & Push Docker Image
    runs-on: ubuntu-24.04
    timeout-minutes: 20

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GIT_ACTION_TOKEN }}
          submodules: true

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Grant execute permission for Gradlew
        run: chmod +x gradlew

      - name: Build JAR
        run: ./gradlew clean bootJar -Pprofile=dev

      - name: Docker Hub Login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 이미지명 소문자 강제(대문자 섞이면 invalid reference format)
      - name: Prepare image tag
        run: |
          IMAGE="${{ secrets.DOCKER_USERNAME }}/${{ secrets.DOCKER_REPO }}:latest"
          echo "IMAGE=${IMAGE,,}" >> "$GITHUB_ENV"

      - name: Build & Push
        run: docker buildx build -t "$IMAGE" --push .

  deploy:
    if: contains('refs/heads/main,refs/heads/develop,refs/heads/cicd', github.ref)
    name: Deploy to Server
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GIT_ACTION_TOKEN }}
          submodules: true

      # 접속 안 되면 바로 fail → 5~10분 매달림 방지
      - name: SSH precheck (fail fast)
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: "echo ok"

      - name: Copy docker-compose.yml to Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -x
            # 1) sshd 살아있는지
            sudo systemctl status ssh --no-pager || sudo systemctl status sshd --no-pager || true
            # 2) 22/TCP 리슨 확인
            sudo ss -lntp | grep :22 || true
            # 3) 방화벽(UFW) 상태
            sudo ufw status || true
            # 4) 최근 인증/접속로그(배포판에 따라 경로 상이)
            if [ -f /var/log/auth.log ]; then
              sudo tail -n 100 /var/log/auth.log
            elif [ -f /var/log/secure ]; then
              sudo tail -n 100 /var/log/secure
            else
              echo "no auth log file"
            fi
          source: "docker-compose.yml"
          target: "~/app"

      - name: Deploy on server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            cd ~/app

            echo "🔐 writing .env"
            printf "%s" "${{ secrets.ENV_FILE }}" > .env

            echo "🚀 compose up"
            docker compose --env-file .env pull || true
            docker compose --env-file .env up -d --build

            echo "🧹 prune"
            docker image prune -f
